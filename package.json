{
  "name": "pinyin-pro-doc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pnpm --filter \"./en\" run docs:build && pnpm --filter \"./zh\" run docs:build && node ./scripts/zip"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "fs-extra": "^11.2.0",
    "zip-a-folder": "^3.1.7"
  }
}
